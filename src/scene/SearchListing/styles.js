import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    floatingFilterButtonStyle: {
        alignSelf: 'flex-end',
        position: 'absolute',
        bottom: 35,
        paddingRight: 15
    }
});
